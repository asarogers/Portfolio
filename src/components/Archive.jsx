import WaveText from "./WaveText";

export default function Archived({ theme }) {
    return (
        <section id="skills" className="skills">
            <WaveText text={"page"} theme={theme} />
        </section>
    )
}

